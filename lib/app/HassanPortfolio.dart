import 'package:demo/screens/Home.dart';
import 'package:flutter/material.dart';

class HassanPortfolio extends StatelessWidget {
  const HassanPortfolio({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Home(),
    );
  }
}